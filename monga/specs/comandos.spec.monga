int foo (int tw)
{
    return 1;
}

int[] func (int num) {
    int[] b, c;
    int x, w;
    if (0) {} else {}

    while (b[1] > c[0]) {}
    while (x > c[0]) {
        b = 1;
    }

    x = c[0];
    func (10);
    w = foo (b[0]);

    {
        b[0] = 1;
    }

    return b;
}
